plugins {
    id "io.qameta.allure" version "2.10.0"
}
apply plugin: 'java'
apply from: "gradle/dependencies.gradle"

allure {
    version = allureVer
    boolean clean = true
    useJUnit5 {
        version = allureVer
    }
}

test {
    useJUnitPlatform()
    finalizedBy 'allureReport'
}