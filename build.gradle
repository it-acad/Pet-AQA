plugins {
    id 'io.qameta.allure' version '2.8.1'
}
apply plugin: 'java'
apply from: "gradle/dependencies.gradle"

allure {
    version = allureVer
    boolean clean = true
    useJUnit5 {
        version = allureVer
    }
}

test {
    useJUnitPlatform()
    finalizedBy 'allureReport'
}